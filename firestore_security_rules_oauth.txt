rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // OAuth Users Collection
    // Stores name and email for Apple/Google users
    // ============================================
    match /oauth_users/{userId} {
      // Allow users to read their own OAuth data
      allow read: if request.auth != null && request.auth.uid == userId;
      
      // Allow users to write their own OAuth data
      // This happens during sign-up and sign-in
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // ============================================
    // Your Existing Rules Below
    // (Keep all your existing rules for students, teachers, admin, etc.)
    // ============================================
    
    // Example: If you have these rules, keep them
    // match /students/{studentId} {
    //   allow read, write: if request.auth != null;
    // }
    
    // match /teachers/{teacherId} {
    //   allow read, write: if request.auth != null;
    // }
    
    // etc...
  }
}
